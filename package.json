{
    "name": "i3-color-theme-editor",
    "version": "0.0.0",
    "main": "index.js",
    "author": "Noah Rosenzweig <rosenzweig.noah@gmail.com>",
    "license": "MIT",
    "scripts": {
        "dev": "NODE_OPTIONS=--openssl-legacy-provider npm-run-all -lnp build:watch serve",
        "build": "NODE_OPTIONS=--openssl-legacy-provider webpack",
        "build:watch": "yarn run build --watch",
        "serve": "live-server ./public --no-browser --port 8080"
    },
    "devDependencies": {
        "live-server": "^1.2.1",
        "npm-run-all": "^4.1.5",
        "prettier": "^2.3.1",
        "ts-loader": "^9.2.3",
        "typescript": "^4.3.4",
        "webpack": "^5.40.0",
        "webpack-cli": "^4.7.2"
    }
}
